import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.involvest.app',
  appName: 'Involvest',
  webDir: 'dist/spa',
  bundledWebRuntime: false
};

export default config;
